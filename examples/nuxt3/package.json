{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "doctor": "clinic doctor --on-port 'autocannon localhost:$PORT' -- node node_modules/nuxi/bin/nuxi.mjs dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "nuxt:prepare": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.10.3"
  }
}
